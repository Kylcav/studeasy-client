import Errors from "../Errors";

/**
 * Studeasy-v2 (mobile): l'onglet "Rank" côté élève correspond à la vue
 * "Erreurs / Mistakes" (historique des questions ratées).
 */
export default function StudentRank() {
  return <Errors />;
}
